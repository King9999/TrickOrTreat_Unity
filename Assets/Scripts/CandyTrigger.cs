using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* This is used by players to collect candy when they touch the hitbox. Each trigger has an ID that matches an identical house ID so that 
 * it's known which house the player is collecting candy from. */
public class CandyTrigger : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
